<article class='newsletter_comment_message' id='<%= "comment-#{comment.to_param}" %>'>
<div class="row">
  <div class="newsletter-comment">

    <div class="col-xs-12">
      <div class="details">
        <span><%= t('by', :scope => 'refinery.newsletter.posts.comments', :who => comment.name) %>,</span>
        <%= t('time_ago', :scope => 'refinery.newsletter.posts.comments', :time => time_ago_in_words(comment.created_at)) %>
      </div>
      <%= simple_format auto_link(comment.message.to_s) %>
    </div>
  </div>
</div>
  
</article>
